# The `utili` package now only carries the vendored `recap` config library
# (imported directly as `utili.recap`). The former camera_config / logger modules
# moved to `ocr_runtime`, and `board_fit` was archived (superseded by
# `ocr_runtime.board_geometry`).
